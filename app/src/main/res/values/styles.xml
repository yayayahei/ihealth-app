<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.button" parent="@android:style/Widget.DeviceDefault.Button.Borderless">
        <item name="android:background">#FFFFFF</item>
        <item name="android:textColor">#03A9F4</item>
        <item name="android:layout_marginTop">16dp</item>
    </style>
    <style name="AppTheme.button.dangerous" parent="@android:style/Widget.DeviceDefault.Button.Borderless">
        <item name="android:background">#FFFFFF</item>
        <item name="android:textColor">#F44336</item>
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:textStyle">bold</item>
    </style>
    <style name="AppTheme.listItem" parent="@android:style/Widget.DeviceDefault.TextView">
        <item name="android:background">@color/cardview_light_background</item>
        <item name="android:gravity">center|start</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:layout_marginBottom">3dp</item>
    </style>
    <style name="AppTheme.listItem.textView" parent="@android:style/Widget.DeviceDefault.TextView">
        <item name="android:background">@color/cardview_light_background</item>
        <item name="android:gravity">center|start</item>
<!--        <item name="android:paddingStart">16dp</item>-->
<!--        <item name="android:paddingEnd">16dp</item>-->
        <item name="android:layout_marginBottom">3dp</item>
    </style>

</resources>
